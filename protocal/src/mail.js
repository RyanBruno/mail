class Mail {
    constructor(reversePath) {
        this.reversePath = reversePath;
    }

    addRecipient(recipient) {
        // Parse recipient
        // Add recipient
    }
}

module.exports = Mail;
